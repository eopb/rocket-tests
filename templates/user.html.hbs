<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="/public/style/style.css">
    <title>Repositories owned by</title>
</head>

<body>
    Repositories owned by {{user}}
    {{#each repos}}
    <h1>Repositories owned by {{name}}</h1>
    <p>{{description}}</p>
    {{/each}}
</body>

</html>